# 功能与兼容性边界

0.3.0：规范整数、精确金额，以及字符串小数和逐字数字双向转换；支持财务大写、显式繁体/异体字，
保留小数精度、末尾零和负零。网页/CLI 均可使用全部转换模式。详细契约见 README。

仍缺混合阿拉伯/中文单位、句子日期/分数/百分比、更多口语规范和可配置金额格式，未判定全面追平。
整数限 16 位数量级，小数最多 64 位，金额精确到分；不接受浮点输入，不隐式舍入。

本轮证据：4 组新增 JS 目标 MoonBit 测试、7 种编译入口固定值检查、长小数 CLI 和错误输入检查。
没有重复全量验证，没有独立 cn2an 程序或浏览器视觉验收。旧 evidence 文件是历史快照。

独立 Git 仓库、构建目录、可执行文档、已有 Wasm-GC/JS 测试、浏览器与 CLI、CI 配置均随仓库交付。
CI 配置不代表远端 CI 已运行。没有公开发布或比赛验收结论。
